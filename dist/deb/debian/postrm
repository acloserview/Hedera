#!/bin/sh

set -e

while read _file; do
	if [ -f "$_file" ];then
		rm -fv "$_file"
	fi
done < /usr/share/Hedera-theme/files
if [ -f /usr/share/themes/Hedera/gtk-3.0/gtk.css ];then
	printf "\nremoval failed - Aborting!\n"
	exit 1
fi
rm -rf /usr/share/Hedera-theme

printf '\n==> Done!\n'
